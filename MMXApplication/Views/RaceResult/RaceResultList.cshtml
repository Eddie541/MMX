@model MMXModelsLib.RaceResultViewModel
@using MMXApplication.Views
@{
    Layout = null;
}

   

    <h2>@Model.Year</h2>
    <table id="yearsRacesTable">
        <tr>      
            <th></th>      
            <th>
                Race
            </th>
            <th>
                Track
            </th>
            <th>
                City
            </th>
            <th>
                Race Date
            </th>
        </tr>
      
    </table>
      @for (int i = 0; i < Model.CompletedRaces.Count(); i++) {

          Html.RenderPartial("CompletedRace", Model.CompletedRaces[i]);
        }        
    <div>
        @using (Html.BeginForm("RaceResultsList", "RaceResult", FormMethod.Post)) {  
            @Html.Partial("YearSelector", Model.YearSelector);
        }
    </div>
    <div>
      @* @if (Model.IsAdminRole && Model.RaceSelector.Races.Count() > 0) {  
           
           using (Html.BeginForm("RaceResultEntry", "RaceResult", FormMethod.Post)) {
            @Html.Partial("RaceSelector", Model.RaceSelector)
           }
       }    *@
    </div>


